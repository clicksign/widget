// Generated by CoffeeScript 1.8.0
(function() {
  var DOCUMENT, HEIGHT_DEFAULT, HEIGHT_MIN, HOST_DEFAULT, ORIGIN, PROTOCOL_DEFAULT, WIDTH_DEFAULT, WIDTH_MIN, attach_element, configure, create_iframe, defaults, document_path, domain_for, host_for, protocol_for;

  WIDTH_MIN = 600;

  WIDTH_DEFAULT = 800;

  HEIGHT_MIN = 500;

  HEIGHT_DEFAULT = 600;

  PROTOCOL_DEFAULT = "https";

  HOST_DEFAULT = "widget.clicksign-demo.com";

  ORIGIN = this.location.origin;

  DOCUMENT = this.document;

  defaults = function(d) {
    return function(v) {
      return v || d;
    };
  };

  protocol_for = defaults(PROTOCOL_DEFAULT);

  host_for = defaults(HOST_DEFAULT);

  domain_for = function(p, h) {
    return "" + (protocol_for(p)) + "://" + (host_for(h));
  };

  document_path = function(k, e) {
    return "/documents/" + k + "?email=" + e + "&origin=" + ORIGIN;
  };

  create_iframe = function(source, width, height) {
    var iframe, min, normalize, normalize_height, normalize_width;
    min = function(m) {
      return function(v) {
        if (v < m) {
          return m;
        } else {
          return v;
        }
      };
    };
    normalize = function(m, d) {
      return function(v) {
        return min(m)(defaults(d)(v));
      };
    };
    normalize_height = normalize(HEIGHT_DEFAULT, HEIGHT_DEFAULT);
    normalize_width = normalize(WIDTH_DEFAULT, WIDTH_DEFAULT);
    iframe = document.createElement("iframe");
    iframe.setAttribute('src', source);
    iframe.setAttribute('width', normalize_width(width));
    iframe.setAttribute('height', normalize_height(height));
    return iframe;
  };

  attach_element = function(base, element) {
    var target;
    target = DOCUMENT.getElementById(base);
    return DOCUMENT.body.insertBefore(element, target.nextSibling);
  };

  configure = function(options) {
    var callback, domain, iframe, path;
    domain = domain_for(options.protocol, options.host);
    path = document_path(options.key, options.email);
    iframe = create_iframe(domain + path, options.width, options.height);
    attach_element(options.base, iframe);
    callback = options.callback || function() {};
    return window.addEventListener("message", callback);
  };

  this.clicksign || (this.clicksign = {
    configure: configure
  });

}).call(this);
